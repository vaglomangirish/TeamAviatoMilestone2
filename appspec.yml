version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
hooks:
  BeforeInstall:
    - location: api-ingestor/scripts/before_install.sh
      timeout: 3600
      runas: root
  ApplicationStart:
    - location: api-ingestor/scripts/install.sh
      timeout: 3600
      runas: root
